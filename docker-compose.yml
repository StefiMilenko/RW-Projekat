services:
  db:
    image: postgres:16
    restart: always
    environment:
      POSTGRES_USER: game_trade
      POSTGRES_PASSWORD: game_trade_pass
      POSTGRES_DB: game_db
    ports:
      - "5432:5432"
    volumes:
      - db_data:/var/lib/postgresql/data
volumes:
  db_data: